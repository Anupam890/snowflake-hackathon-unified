"""Shared helpers for the INSIGHT AI Streamlit pages.

Both `app/streamlit_app.py` and `app/pages/1_Enterprise_AI.py` render the same
chat experience. These modules hold the single implementation of that shared
behaviour so a fix lands in one place rather than two.
"""
